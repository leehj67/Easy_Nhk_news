{
  "name": "nhk-easy-reader-app",
  "version": "1.0.0",
  "description": "NHK Easy Japanese Reader - Android 앱",
  "scripts": {
    "build": "node build.js && npx cap sync android",
    "open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  },
  "capacitor": {
    "android": {
      "path": "android"
    }
  }
}
